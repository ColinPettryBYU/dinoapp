# Frontend Notes

This folder contains the React + Vite client.

For full project setup and run instructions (frontend + backend + database), see the root guide:

- `../README.md`

Quick frontend-only run:

```bash
cd frontend
npm install
npm run dev
```

Frontend default URL: `http://localhost:8080`
